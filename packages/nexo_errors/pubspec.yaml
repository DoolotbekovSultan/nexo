name: nexo_errors
version: 0.1.0
description: Failure model, error mappers, and localization for nexo.
repository: https://github.com/DoolotbekovSultan/nexo

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  nexo_logger:
    path: ../nexo_logger
  dio: ^5.10.0
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.13.1
  freezed: ^4.0.0
