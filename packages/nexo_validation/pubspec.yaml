name: nexo_validation
version: 0.1.0
description: Form validators for nexo.
repository: https://github.com/DoolotbekovSultan/nexo

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  nexo_errors:
    path: ../nexo_errors
  flutter:
    sdk: flutter
