name: nexo_errors_firebase
version: 0.1.0
description: Firebase Auth and Messaging failure mappers for nexo_errors.
repository: https://github.com/DoolotbekovSultan/nexo

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  nexo_errors:
    path: ../nexo_errors
  firebase_auth: ^6.3.0
  firebase_core: ^4.6.0
  flutter:
    sdk: flutter
