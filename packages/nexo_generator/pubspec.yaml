name: nexo_generator
version: 0.0.8-beta.0
description: Code generator for nexo package. Generates UseCase implementations from @NexoUseCaseAnnotation.
repository: https://github.com/DoolotbekovSultan/nexo

environment:
  sdk: ^3.11.3

dependencies:
  analyzer: ">=5.12.0 <15.0.0"
  build: ">=2.4.0 <5.0.0"
  source_gen: ">=2.0.0 <5.0.0"

dev_dependencies:
  build_test: ^2.2.0
  test: ^1.24.0
